<%= stylesheet_link_tag 'application' %>  
<h1>New Fight</h1>
<%= form_for :fight, url: fights_path do |f| %>


  <% if @fight.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@fight.errors.count, "error") %> prohibited
        this fight from being saved:
      </h2>
      <ul>
        <% @fight.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', fights_path %>